import { configureStore } from '@reduxjs/toolkit';
// import datePickerReducer from '../src/Components/DatePickerSlice'

export default configureStore({
  reducer: {
    // datePicker: datePickerReducer,
  },
});
